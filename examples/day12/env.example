# 僅格式範例，實際檔案放 Repo 外、chmod 600。不要把真實值貼進對話或 git。
export LOCAL_BACKEND=emulator
export LOCAL_MODEL_MODE=stub
export LOCAL_NAMESPACE=day11-d12-author-test
export GOOGLE_CLOUD_PROJECT=demo-local-day12
export FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
export LOCAL_APPROVE_EXTERNAL=no
export LOCAL_MODEL_DAILY_LIMIT=10
export LINE_DESTINATION='填入測試 Bot 的 userId'
export LINE_ALLOWED_USER_IDS='填入已同意測試者的 LINE userId，以逗號分隔'
export LINE_CHANNEL_SECRET='填入私人值'
export LINE_CHANNEL_ACCESS_TOKEN='填入私人值'
export LOCAL_ACTOR_KEY='以安全亂數產生並持久保存，至少32bytes，跨修訂版不更換'
# 真實 Gemini：核准後 model_mode=gemini、approve_external=yes，再填模型與 key。
# 正式 Firestore：backend=cloud，project 換成核准專案，unset FIRESTORE_EMULATOR_HOST。
